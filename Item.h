#include <string>
using namespace std;
struct Item
{
  int consonants; // number of consonants in key
  int vowels;     // number of vowels in key
  int count;      // number of keys stored at this location
  string key;
};
